<div ng-app="app">
  <div ng-controller="bookratingsCtrl" ng-init="setup(<%= params[:id] %>)">
<section class="main-container">

  <div class="container">
    <div class="row">

      <!-- main start -->
      <!-- ================ -->
      <div class="main col-md-12">

        <!-- page-title start -->
        <!-- ================ -->
        <h2 class="page-title"><%= @book.title %></h2>
        <div class="separator-2"></div>
        <!-- page-title end -->

        <div class="row">
          <div class="col-md-4">
            <!-- pills start -->
            <!-- ================ -->
            <!-- Nav tabs -->
            <!-- <ul class="nav nav-pills" role="tablist">
              <li class="active"><a href="#pill-1" role="tab" data-toggle="tab" title="images"><i class="fa fa-camera pr-5"></i> Photo</a></li>

            </ul> -->
            <!-- Tab panes -->
            <div class="tab-content clear-style">
              <div class="tab-pane active" id="pill-1">
                <div class="owl-carousel content-slider-with-large-controls">
                  <div class="overlay-container overlay-visible">

                    <a href="images/product-1.jpg" class="popup-img overlay-link" title="image title"><i class="icon-plus-1"></i></a>
                  </div>

                </div>

              </div>
            </div>
            <div>
              <img src="<%= @book.image.link %>" alt="">
              <h4> Author: <%= @book.author %></h4>
              <h4> Genre: <%= @book.genre %></h4>
            </div>
            <!-- pills end -->
          </div>
          <div class="col-md-8 pv-30">
            <h4>Description</h4>
            <p class="description"><%= @book.description %> </p>
            <hr class="mb-10">
            <div class="clearfix mb-20">
              <span>
                <i class="star-rating fa fa-lg" ng-repeat="star in book.stars" ng-click="favoriteBook(book, star)" ng-mouseover="fillStar(book, star)" ng-mouseleave="emptyStar(book, star)" ng-class="{'fa-star': star.filled, 'fa-star-o': !star.filled}" aria-hidden="true"></i>
              </span>
            </div>
            <div class="row grid-space-10">

                    <div class="col-md-4">
                    <% if current_user %>
                        <%= button_to "Add to favorite", "/favorites/#{@book.id}", {method: :post, class: "btn btn-success"} %>
                        <%= button_to "Add to reading list", "/lists/#{@book.id}", {method: :post, class: "btn btn-success"} %>
                    <% end %>
                    </div>


            </div>

          </div>
        </div>
      </div>
      <!-- main end -->

    </div>
  </div>
</section>
<!-- main-container end -->
